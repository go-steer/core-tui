# Acknowledged incompatible changes to the exported API since the last
# release tag. Read and enforced by dev/tools/verify-apidiff.
#
# One apidiff line per entry, exactly as the tool prints it under
# "Incompatible changes" but without the leading "- ". Blank lines and
# everything after a `#` are ignored. For example:
#
#   # #77: Options.Foo never had a caller outside the examples and its
#   # zero value was already the only supported one.
#   ./tui.Options.Foo: removed
#
# An entry here is not a rubber stamp — it is a claim, made in the PR that
# breaks the symbol, that the break is intended and written down. Record
# the same change under Changed or Removed in CHANGELOG.md.
#
# Cutting a release empties this file: once the tag moves, these breaks
# sit behind the new baseline, and a leftover entry would silently permit
# a second, unrelated removal of a symbol that no longer exists.
#
# Nothing to empty at v0.25.0 either, and for the opposite reason to the
# usual one: that cycle only added. #313 put six symbols on the exported
# surface (GuardrailTrip and its two guardrail-name constants, the
# TurnErrorCanceled kind, and the GuardrailTrip fields on Event and
# Message), which apidiff reports under Compatible changes and this gate
# does not read. Minor rather than patch for exactly that reason.
#
# Nothing to empty at v0.24.1 either: a fix-only patch release, and
# verify-apidiff reported no change to the exported API across the whole
# cycle. #305 added a model field and a method, both unexported.
#
# Nothing to empty at v0.24.0: that cycle broke no exported Go symbol,
# so the file went into the tag empty and came out of it empty. The one
# commit marked breaking there (#268) renames an operator-facing slash
# command, which this gate does not watch and could not see.
#
# Emptied at v0.23.0. The three entries that were here are behind that
# tag now: #115's Model / NewModel pair, the last exported type whose
# fate was a break either way, with the recipe in MIGRATION.md §7 and
# the reasoning in docs/decisions.md D18a; and #260's fakehost client
# signature, in the file only because the apidiff gate walks every
# package in the module. All three are written up under Changed in the
# v0.23.0 section of CHANGELOG.md. The 14 emptied at v0.22.0 and the 38
# at v0.21.0 are one and two baselines further back.
#
# It is emptied after the tag moves rather than in the release PR: the
# entries acknowledge breaks measured against the previous baseline, so
# clearing them any earlier would leave verify-apidiff reporting the
# same removals with nothing to match them against.

# Entries since v0.25.0 follow.
